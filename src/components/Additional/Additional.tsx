import React from 'react';

import './Additional.sass';

export default function Additional({title}: {title: string}) {
    return (
        <div className='Additional'>
            {title}
        </div>
    )
}
